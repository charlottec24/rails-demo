<h1>Log In Below</h1>

<%= form_for :user, :url => login_path do |f| %>
	<div>
		<%= f.label :user_name %>				
		<%= f.text_field :user_name %> 
	</div>
	<div>
		<%= f.label :password %>
		<%= f.password_field :password %>
  	</div>
  	<div>
  		<%= f.submit "Login" %>
  	</div>
<% end %>

